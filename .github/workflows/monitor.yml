name: Website Monitor

on:
workflow_dispatch:

jobs:
test:
runs-on: ubuntu-latest
steps:
- run: |
curl -s -X POST "api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" 
-d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" 
-d "text=✅ GitHub monitor test successful!"
