name: Website Monitor

on: [workflow_dispatch]

jobs:
  kicker-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Playwright
        run: |
          pip install playwright
          playwright install --with-deps chromium

      - name: Test Kicker
        run: python kicker_test.py
